# 裂隙尖塔

一个从零搭建的杀戮尖塔风格卡牌肉鸽原型，包含随机路线、遭遇战、卡牌构筑、药水、遗物、常驻提升、事件、商店、营火、多难度和 Boss 流程。

## 本地运行

```bash
npm install
npm run dev
```

默认开发地址是 `http://localhost:5188/`。

## 检查

```bash
npm run build
npm run smoke
npm run balance
```

## 部署

推送到 `main` 分支后，GitHub Actions 会自动构建并发布到 GitHub Pages。
